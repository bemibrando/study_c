// Standard inpud and output library
#include <stdio.h>
// Standard library for memory management, system funcions, etc.
#include <stdlib.h>

// Main function: Entry point of the program
int main() {
	// Prints "Hello World!" to the console
	printf("Hello World!\n");

	// Keeps the console window open until the user presses a key (Windows only)
	system("pause");

	// Returns 0, indicating that the program executed successfully
	return 0;
}